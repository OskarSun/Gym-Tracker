export interface UserData {
    username: string;
    password: string;
}

export interface Token {
    token: string;
}

export interface Workout {
    id: number;
    name: string;
    date: string;
}